# Under Master Database, 
# - current tab goes subtab as "Live Database" (Delete all the download button)
# - create 1 more tab, called "Master Database"  (Add all the download button; rds, xlsx, csv)
# - this new tab will be uploaded by data.R
# - This data needs another code in data.R as ofr_master_data variable. 
# - that will be. rbind(ofr_original, master_data)
# - distinct by ref. but, always priority if either reason_code, comment has value. 


